export const Styles = ({ darkMode }) => ({
  iconFill: {
    fill: "white",
    height: "40px",
    width: "40px",
    cursor: "pointer",
  },
  leftArrow:{
    fill: darkMode ? "white" : "black",
    height: "40px",
    width: "40px",
    cursor: "pointer",
  },
  
});
